<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PracticumAndroidDiploma" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryDark">@color/black_universal</item>
        <item name="colorAccent">@color/blue</item>
        <item name="colorOnBackground">@color/black_universal</item>

        <!-- Цвета текста -->
        <color name="textColorPrimary">@color/white_universal</color>
        <color name="textColorSecondary">@color/white_universal</color>
        <color name="hintColor">@color/gray</color>

        <!-- Цвет фона окна -->
        <color name="windowBackground">@color/black_universal</color>
    </style>
</resources>
